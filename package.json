{
  "name": "kratos",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "biome check",
    "lint:fix": "biome check --fix --formatter-enabled=true",
    "registry:build": "turbo run registry:build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
