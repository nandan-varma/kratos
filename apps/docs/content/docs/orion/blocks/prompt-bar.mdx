---
title: Prompt Bar
description: A composer with @ sources, / commands, a model picker, and dictation.
---

import { PromptBar } from "@/components/prompt-bar";
import { ComponentPreview } from "@/components/component-preview";
import { getComponentSource } from "@/lib/get-source";

<ComponentPreview code={getComponentSource("components/prompt-bar.tsx")}>
  <PromptBar />
</ComponentPreview>

## Installation

```sh
pnpm dlx shadcn@latest add @kratos-orion/prompt-bar
```

## Usage

Type `@` to open the sources menu or `/` for commands. `variant` accepts `"Rounded"` (default) or `"Pill"`.
