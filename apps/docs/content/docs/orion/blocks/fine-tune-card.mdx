---
title: Fine-tune Card
description: An inspector panel with scrubbable number fields and a segmented control.
---

import { FineTuneCard } from "@/components/fine-tune-card";
import { ComponentPreview } from "@/components/component-preview";
import { getComponentSource } from "@/lib/get-source";

<ComponentPreview code={getComponentSource("components/fine-tune-card.tsx")}>
  <FineTuneCard />
</ComponentPreview>

## Installation

```sh
pnpm dlx shadcn@latest add @kratos-orion/fine-tune-card
```

## Usage

The `W` / `H` / `Radius` / `Opacity` labels are scrub handles — drag horizontally, or focus and use arrow keys (shift for ×10).
